---
trigger: always_on
---

当你成功完成了计划，你需要将实现计划、任务和 walkthrough 合并成一个文档进行落盘，写入到当前项目的 agent_works 目录下，markdown 格式，文件名格式为 `yyyy-MM-dd_{title}.md`，也就是年月日加上标题。如果计划后面又有补充、变更等修改操作，也请一并同步到此文件中，注意不要将隐私信息进行落盘。
