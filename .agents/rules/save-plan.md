---
trigger: always_on
---

当你完成了计划并且用户和你说计划已验证/已完成（如果用户没有明确说当前计划已验证/已完成，**不能**自动落盘），你需要将实现计划、任务和 walkthrough 一起进行落盘，写入到当前项目的 agent_works 目录下，markdown 格式，文件名格式为 `yyyy-MM-dd_{title}.md`，也就是年月日加上标题。如果计划后面又有补充、变更等修改操作，也请一并同步到此文件中。