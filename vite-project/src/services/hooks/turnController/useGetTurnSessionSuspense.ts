/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import type {
  GetTurnSessionQueryResponse,
  GetTurnSessionQueryParams,
} from "../../types.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";
import { getTurnSession } from "../../clients.ts";

export const getTurnSessionSuspenseQueryKey = (
  params: GetTurnSessionQueryParams,
) => [{ url: "/api/turn/session" }, ...(params ? [params] : [])] as const;

export type GetTurnSessionSuspenseQueryKey = ReturnType<
  typeof getTurnSessionSuspenseQueryKey
>;

export function getTurnSessionSuspenseQueryOptions(
  params: GetTurnSessionQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getTurnSessionSuspenseQueryKey(params);
  return queryOptions<
    GetTurnSessionQueryResponse,
    ResponseErrorConfig<Error>,
    GetTurnSessionQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getTurnSession(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @summary Get turn server session
 * {@link /api/turn/session}
 */
export function useGetTurnSessionSuspense<
  TData = GetTurnSessionQueryResponse,
  TQueryKey extends QueryKey = GetTurnSessionSuspenseQueryKey,
>(
  params: GetTurnSessionQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetTurnSessionQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? getTurnSessionSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...getTurnSessionSuspenseQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
