/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import type { ListConnectionsQueryResponse } from "../../types.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";
import { listConnections } from "../../clients.ts";

export const listConnectionsQueryKey = () =>
  [{ url: "/api/desk/connections" }] as const;

export type ListConnectionsQueryKey = ReturnType<
  typeof listConnectionsQueryKey
>;

export function listConnectionsQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = listConnectionsQueryKey();
  return queryOptions<
    ListConnectionsQueryResponse,
    ResponseErrorConfig<Error>,
    ListConnectionsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return listConnections({ ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @summary List all online desk connections
 * {@link /api/desk/connections}
 */
export function useListConnections<
  TData = ListConnectionsQueryResponse,
  TQueryData = ListConnectionsQueryResponse,
  TQueryKey extends QueryKey = ListConnectionsQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        ListConnectionsQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? listConnectionsQueryKey();

  const query = useQuery(
    {
      ...listConnectionsQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
