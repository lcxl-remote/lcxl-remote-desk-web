/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/kubb-client";
import type {
  UseMutationOptions,
  UseMutationResult,
  QueryClient,
} from "@tanstack/react-query";
import type { InstallDriverMutationResponse } from "../../types.ts";
import { mutationOptions, useMutation } from "@tanstack/react-query";
import { installDriver } from "../../clients.ts";

export const installDriverMutationKey = () =>
  [{ url: "/api/virtual-display/driver/install" }] as const;

export type InstallDriverMutationKey = ReturnType<
  typeof installDriverMutationKey
>;

export function installDriverMutationOptions<TContext = unknown>(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const mutationKey = installDriverMutationKey();
  return mutationOptions<
    InstallDriverMutationResponse,
    ResponseErrorConfig<Error>,
    void,
    TContext
  >({
    mutationKey,
    mutationFn: async () => {
      return installDriver(config);
    },
  });
}

/**
 * @summary Install LcxlVirtualDisplay IDD driver
 * {@link /api/virtual-display/driver/install}
 */
export function useInstallDriver<TContext>(
  options: {
    mutation?: UseMutationOptions<
      InstallDriverMutationResponse,
      ResponseErrorConfig<Error>,
      void,
      TContext
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { mutation = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...mutationOptions } = mutation;
  const mutationKey = mutationOptions.mutationKey ?? installDriverMutationKey();

  const baseOptions = installDriverMutationOptions(
    config,
  ) as UseMutationOptions<
    InstallDriverMutationResponse,
    ResponseErrorConfig<Error>,
    void,
    TContext
  >;

  return useMutation<
    InstallDriverMutationResponse,
    ResponseErrorConfig<Error>,
    void,
    TContext
  >(
    {
      ...baseOptions,
      mutationKey,
      ...mutationOptions,
    },
    queryClient,
  ) as UseMutationResult<
    InstallDriverMutationResponse,
    ResponseErrorConfig<Error>,
    void,
    TContext
  >;
}
