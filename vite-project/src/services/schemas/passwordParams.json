{
  "type": "object",
  "description": "Password params",
  "required": ["username", "password"],
  "properties": {
    "new_password": {
      "type": ["string", "null"],
      "description": "New password (optional)"
    },
    "new_username": {
      "type": ["string", "null"],
      "description": "New username (optional)"
    },
    "password": { "type": "string", "description": "Old password" },
    "username": { "type": "string", "description": "Old username" }
  },
  "x-readme-ref-name": "PasswordParams"
}
