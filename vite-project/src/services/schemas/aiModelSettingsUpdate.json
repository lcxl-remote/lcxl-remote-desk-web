{
  "type": "object",
  "description": "Update body for `POST /api/desk/settings/ai-model`.\n\nEvery field is optional: `None` leaves the stored value unchanged. `api_key`\nis write-only with three-way semantics (see [`AiModelSettings::apply_update`]).",
  "properties": {
    "allow_logs": { "type": ["boolean", "null"] },
    "allow_screen": { "type": ["boolean", "null"] },
    "api_key": {
      "type": ["string", "null"],
      "description": "Write-only. `None` = leave unchanged; `Some(\"\")` = clear; `Some(x)` = set."
    },
    "base_url": { "type": ["string", "null"] },
    "max_context_bytes": {
      "type": ["integer", "null"],
      "format": "int64",
      "minimum": 0
    },
    "model": { "type": ["string", "null"] },
    "provider": { "type": ["string", "null"] },
    "response_format": {
      "oneOf": [
        { "type": "null" },
        {
          "type": "string",
          "description": "`None` leaves the stored mode unchanged.",
          "enum": ["none", "json_object", "json_schema"],
          "x-readme-ref-name": "ResponseFormatMode"
        }
      ]
    }
  },
  "x-readme-ref-name": "AiModelSettingsUpdate"
}
