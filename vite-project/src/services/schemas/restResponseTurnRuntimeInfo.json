{"type":"object","required":["success","code"],"properties":{"code":{"type":"integer","format":"int32"},"data":{"type":"object","description":"Runtime status of the TURN service on this host.\n\nReports what is *running*, never what is configured — the configuration is\nserved by the settings endpoint, and conflating the two is how an operator\nends up believing a relay is up because it was saved.","required":["state","software","interfaces","rejected_interfaces"],"properties":{"interfaces":{"type":"array","items":{"type":"object","required":["transport","listen","external"],"properties":{"external":{"type":"string","description":"external address\n\nspecify the node external address and port.\nfor the case of exposing the service to the outside,\nyou need to manually specify the server external IP\naddress and service listening port."},"listen":{"type":"string","description":"turn server listen address"},"transport":{"type":"string","enum":["tcp","udp"],"x-readme-ref-name":"TurnTransport"}},"x-readme-ref-name":"TurnInterface"},"description":"The interfaces the running runtime serves; empty unless\n[`TurnRuntimeState::Running`]."},"last_error":{"type":["string","null"],"description":"Why the last start attempt failed; `None` unless\n[`TurnRuntimeState::Failed`]."},"rejected_interfaces":{"type":"array","items":{"type":"object","description":"A configured interface that will not be served, and what to change.","required":["index","interface","fault","detail"],"properties":{"detail":{"type":"string","description":"What is wrong and what a working entry looks like."},"fault":{"type":"string","description":"Why a configured interface is not served.","enum":["transport-not-served","listen-not-an-address","external-not-an-address","external-not-dialable"],"x-readme-ref-name":"TurnInterfaceFault"},"index":{"type":"integer","description":"Position in the configured interface list, so an operator can find the\nentry even when several look alike.","minimum":0},"interface":{"type":"object","required":["transport","listen","external"],"properties":{"external":{"type":"string","description":"external address\n\nspecify the node external address and port.\nfor the case of exposing the service to the outside,\nyou need to manually specify the server external IP\naddress and service listening port."},"listen":{"type":"string","description":"turn server listen address"},"transport":{"type":"string","enum":["tcp","udp"],"x-readme-ref-name":"TurnTransport"}},"x-readme-ref-name":"TurnInterface","description":"The entry as configured, echoed back unrepaired."}},"x-readme-ref-name":"RejectedTurnInterface"},"description":"Configured interfaces this host refuses to serve, and why.\n\nReported in every state, because it is exactly when the runtime is *not*\nrunning that the reason matters most — a host whose every interface was\nrejected reports [`TurnRuntimeState::NotConfigured`], which on its own\nwould read as \"you configured nothing\" to an operator who configured\nthree."},"software":{"type":"string","description":"Build identifier of this TURN implementation."},"state":{"type":"string","description":"Why this process is or is not relaying right now.\n\n\"Not relaying\" is an answer, not a failure, so the states name the reason\ninstead of collapsing into one error: an operator who switched TURN off, a\nstartup mode that never hosts it, and a host that cannot start the runtime\nneed different things done about them.","enum":["running","disabled","unsupported","not-configured","failed"],"x-readme-ref-name":"TurnRuntimeState"},"uptime_secs":{"type":["integer","null"],"format":"int64","description":"Seconds since the running runtime started; `None` unless\n[`TurnRuntimeState::Running`].","minimum":0}}},"message":{"type":["string","null"]},"success":{"type":"boolean"}},"x-readme-ref-name":"RestResponse_TurnRuntimeInfo"}